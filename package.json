{
  "name": "clipmaster-pro",
  "version": "1.0.0",
  "private": true,
  "description": "ClipMaster Pro — smart clipboard segmenting, synced to Baserow via a small Express server.",
  "main": "server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
